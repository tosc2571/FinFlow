# Override for building the image locally instead of pulling it from GHCR — see the README's
# Docker section. Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build

services:
  finflow:
    build: .
